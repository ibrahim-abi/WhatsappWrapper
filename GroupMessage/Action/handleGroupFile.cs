namespace GroupMessage.Action
{
    internal class handleGroupFile
    {
        public void sendGroupFile(string groupCode, string message, string path)
        {
            //var chromeOptions = Configuration.GetChromeOptions();
            //using (var driver = new ChromeDriver(chromeOptions))
            //{
            //    driver.Navigate().GoToUrl($"https://web.whatsapp.com/accept?code={groupCode}");

            //    IWebElement element = WaitForElement.Wait(driver, By.XPath("//*[@id='main']/footer/div[1]/div/span[2]/div/div[2]/div[1]/div[2]/div[1]/p"), TimeSpan.FromSeconds(30));

            //    if (element != null)
            //    {
            //        element.Click();
            //        element.SendKeys(message + Keys.Enter);

            //        Thread.Sleep(2000);
            //    }
            //    else
            //    {
            //        Console.WriteLine("Element not found within the specified timeout.");
            //    }

            //    driver.Quit();
            //}
        }
    }
}
